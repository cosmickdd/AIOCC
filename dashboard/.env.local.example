# Copy to .env.local and edit for local dev
# NEXT_PUBLIC_API_URL is used by the Next.js build. Use the backend origin (including scheme), e.g.
# NEXT_PUBLIC_API_URL=http://localhost:8000

NEXT_PUBLIC_API_URL=http://localhost:8000
